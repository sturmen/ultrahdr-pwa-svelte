# Backlog

- Support multi-channel gain maps
- Per-image rotation control
- WebNN support (speedup?)
- Parallelizable processing
- Display all input images in media grid with 10% opacity, then overlay progress for each one as a circular progress bar as it is completed.